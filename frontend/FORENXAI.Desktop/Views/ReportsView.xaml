<UserControl x:Class="FORENXAI.Desktop.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0F172A">

    <UserControl.Resources>

        <Style x:Key="CardStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="#111827"/>
            <Setter Property="CornerRadius"
                    Value="12"/>
            <Setter Property="Padding"
                    Value="16"/>
            <Setter Property="Margin"
                    Value="0,0,12,12"/>
            <Setter Property="BorderBrush"
                    Value="#1E293B"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
        </Style>

        <Style x:Key="SectionTitleStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#F8FAFC"/>
            <Setter Property="FontSize"
                    Value="18"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Margin"
                    Value="0,0,0,12"/>
        </Style>

        <Style x:Key="LabelStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#94A3B8"/>
            <Setter Property="FontSize"
                    Value="12"/>
        </Style>

        <Style x:Key="ValueStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#F8FAFC"/>
            <Setter Property="FontSize"
                    Value="15"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="TextWrapping"
                    Value="Wrap"/>
        </Style>

        <Style x:Key="SummaryNumberStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#F8FAFC"/>
            <Setter Property="FontSize"
                    Value="28"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
        </Style>

        <Style x:Key="SummaryLabelStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#94A3B8"/>
            <Setter Property="FontSize"
                    Value="12"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
        </Style>

        <Style x:Key="ReportCellStyle"
               TargetType="DataGridCell">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Foreground"
                    Value="#E2E8F0"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Padding"
                    Value="8,6"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}"/>

            <Style.Triggers>

                <DataTrigger Binding="{Binding IsMouseOver,
                              RelativeSource={RelativeSource AncestorType=DataGridRow}}"
                             Value="True">
                    <Setter Property="Background"
                            Value="#2B5D87"/>
                    <Setter Property="Foreground"
                            Value="White"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding IsSelected,
                              RelativeSource={RelativeSource AncestorType=DataGridRow}}"
                             Value="True">
                    <Setter Property="Background"
                            Value="#60A5FA"/>
                    <Setter Property="Foreground"
                            Value="#0F172A"/>
                    <Setter Property="FontWeight"
                            Value="SemiBold"/>
                </DataTrigger>

            </Style.Triggers>
        </Style>

        <Style x:Key="ReportRowStyle"
               TargetType="DataGridRow">
            <Setter Property="Background"
                    Value="#0F172A"/>
            <Setter Property="Foreground"
                    Value="#E2E8F0"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="MinHeight"
                    Value="36"/>

            <Style.Triggers>

                <Trigger Property="AlternationIndex"
                         Value="1">
                    <Setter Property="Background"
                            Value="#111827"/>
                </Trigger>

            </Style.Triggers>
        </Style>

    </UserControl.Resources>


    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Page header -->

        <Grid Grid.Row="0"
              Margin="0,0,0,20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">

                <TextBlock Text="Forensic Report"
                           Foreground="#F8FAFC"
                           FontSize="28"
                           FontWeight="Bold"/>

                <TextBlock Text="Review case findings, investigator decisions, and report-ready evidence."
                           Foreground="#94A3B8"
                           FontSize="13"
                           Margin="0,5,0,0"/>

            </StackPanel>


            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                <TextBox x:Name="CaseIdTextBox"
                         Width="230"
                         Height="38"
                         Margin="0,0,10,0"
                         Padding="10,6"
                         Background="#111827"
                         Foreground="#F8FAFC"
                         BorderBrush="#334155"
                         BorderThickness="1"
                         VerticalContentAlignment="Center"/>

                <Button x:Name="LoadReportButton"
                        Content="LOAD REPORT"
                        Width="120"
                        Height="38"
                        Click="LoadReportButton_Click"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"/>

            </StackPanel>

        </Grid>


        <!-- Case information -->

        <Border Grid.Row="1"
                Style="{StaticResource CardStyle}">

            <StackPanel>

                <TextBlock Text="Case Information"
                           Style="{StaticResource SectionTitleStyle}"/>

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Margin="0,0,20,0">

                        <TextBlock Text="Case ID"
                                   Style="{StaticResource LabelStyle}"/>

                        <TextBlock x:Name="CaseIdValueText"
                                   Text="--"
                                   Style="{StaticResource ValueStyle}"/>

                    </StackPanel>


                    <StackPanel Grid.Column="1"
                                Margin="0,0,20,0">

                        <TextBlock Text="Evidence File"
                                   Style="{StaticResource LabelStyle}"/>

                        <TextBlock x:Name="EvidenceFileValueText"
                                   Text="--"
                                   Style="{StaticResource ValueStyle}"/>

                    </StackPanel>


                    <StackPanel Grid.Column="2">

                        <TextBlock Text="File Size"
                                   Style="{StaticResource LabelStyle}"/>

                        <TextBlock x:Name="FileSizeValueText"
                                   Text="--"
                                   Style="{StaticResource ValueStyle}"/>

                    </StackPanel>

                </Grid>


                <StackPanel Margin="0,16,0,0">

                    <TextBlock Text="SHA-256 Evidence Hash"
                               Style="{StaticResource LabelStyle}"/>

                    <TextBlock x:Name="Sha256ValueText"
                               Text="--"
                               Style="{StaticResource ValueStyle}"
                               FontFamily="Consolas"
                               FontSize="12"/>

                </StackPanel>

            </StackPanel>

        </Border>


        <!-- Summary cards -->

        <Grid Grid.Row="2"
              Margin="0,0,0,12">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Border Grid.Column="0"
                    Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="TotalFlowsText"
                               Text="0"
                               Style="{StaticResource SummaryNumberStyle}"/>
                    <TextBlock Text="ML Flows"
                               Style="{StaticResource SummaryLabelStyle}"/>
                </StackPanel>
            </Border>


            <Border Grid.Column="1"
                    Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="BenignFlowsText"
                               Text="0"
                               Style="{StaticResource SummaryNumberStyle}"/>
                    <TextBlock Text="Benign"
                               Style="{StaticResource SummaryLabelStyle}"/>
                </StackPanel>
            </Border>


            <Border Grid.Column="2"
                    Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="ThreatFlowsText"
                               Text="0"
                               Style="{StaticResource SummaryNumberStyle}"/>
                    <TextBlock Text="Threats"
                               Style="{StaticResource SummaryLabelStyle}"/>
                </StackPanel>
            </Border>


            <Border Grid.Column="3"
                    Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="ConfirmedReviewsText"
                               Text="0"
                               Style="{StaticResource SummaryNumberStyle}"/>
                    <TextBlock Text="Confirmed"
                               Style="{StaticResource SummaryLabelStyle}"/>
                </StackPanel>
            </Border>


            <Border Grid.Column="4"
                    Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="RejectedReviewsText"
                               Text="0"
                               Style="{StaticResource SummaryNumberStyle}"/>
                    <TextBlock Text="Rejected"
                               Style="{StaticResource SummaryLabelStyle}"/>
                </StackPanel>
            </Border>


            <Border Grid.Column="5"
                    Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="InconclusiveReviewsText"
                               Text="0"
                               Style="{StaticResource SummaryNumberStyle}"/>
                    <TextBlock Text="Inconclusive"
                               Style="{StaticResource SummaryLabelStyle}"/>
                </StackPanel>
            </Border>

        </Grid>


        <!-- Report status / export controls -->

        <Border Grid.Row="3"
                Style="{StaticResource CardStyle}">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">

                    <TextBlock Text="Report Status"
                               Style="{StaticResource LabelStyle}"/>

                    <TextBlock x:Name="ReportStatusText"
                               Text="No report loaded."
                               Style="{StaticResource ValueStyle}"
                               Margin="0,4,0,0"/>

                </StackPanel>


                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <Button x:Name="ExportJsonButton"
                            Content="EXPORT JSON"
                            Width="120"
                            Height="38"
                            Margin="0,0,10,0"
                            IsEnabled="False"
                            Click="ExportJsonButton_Click"
                            Background="#334155"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"/>

                    <Button x:Name="ExportPdfButton"
                            Content="EXPORT PDF"
                            Width="120"
                            Height="38"
                            IsEnabled="False"
                            Click="ExportPdfButton_Click"
                            Background="#D4A017"
                            Foreground="#0F172A"
                            BorderThickness="0"
                            FontWeight="SemiBold"/>

                </StackPanel>

            </Grid>

        </Border>


        <!-- Threat findings -->

        <Border Grid.Row="4"
                Style="{StaticResource CardStyle}"
                Margin="0">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Margin="0,0,0,12">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Threat Findings"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0"/>

                    <TextBlock Grid.Column="1"
                               x:Name="ThreatFindingCountText"
                               Text="0 findings"
                               Foreground="#94A3B8"
                               VerticalAlignment="Center"/>

                </Grid>


                <DataGrid Grid.Row="1"
                          x:Name="ThreatFindingsDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          AlternationCount="2"
                          Background="#0F172A"
                          BorderBrush="#1E293B"
                          BorderThickness="1"
                          RowStyle="{StaticResource ReportRowStyle}"
                          CellStyle="{StaticResource ReportCellStyle}"
                          HorizontalGridLinesBrush="Transparent"
                          VerticalGridLinesBrush="Transparent">

                    <DataGrid.ColumnHeaderStyle>

                        <Style TargetType="DataGridColumnHeader">

                            <Setter Property="Background"
                                    Value="#1E293B"/>

                            <Setter Property="Foreground"
                                    Value="#CBD5E1"/>

                            <Setter Property="FontWeight"
                                    Value="SemiBold"/>

                            <Setter Property="Padding"
                                    Value="8"/>

                            <Setter Property="BorderThickness"
                                    Value="0"/>

                        </Style>

                    </DataGrid.ColumnHeaderStyle>


                    <DataGrid.Columns>

                        <DataGridTextColumn Header="Flow"
                                            Binding="{Binding FlowIndex}"
                                            Width="80"/>

                        <DataGridTextColumn Header="Prediction"
                                            Binding="{Binding PredictedClass}"
                                            Width="160"/>

                        <DataGridTextColumn Header="Confidence"
                                            Binding="{Binding ConfidenceDisplay}"
                                            Width="120"/>

                        <DataGridTextColumn Header="Review"
                                            Binding="{Binding ReviewDecision}"
                                            Width="130"/>

                        <DataGridTextColumn Header="Recommendation"
                                            Binding="{Binding RecommendationSummary}"
                                            Width="*"/>

                    </DataGrid.Columns>

                </DataGrid>

            </Grid>

        </Border>

    </Grid>

</UserControl>